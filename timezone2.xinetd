# default: on
# description: Timezone server

service GUI_Timezone_nw
{
          flags = REUSE
          socket_type = stream
          wait = no
          user = nobody
          server = /usr/local/bin/GUI_Timezone_nw
          disable = yes
}
